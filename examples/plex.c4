plex Color {
  r: x8,
  g: x8,
  b: x8,
  a: x8,
}

proc printf(fmt: ^x8, r:x8,g:x8,b:x8,a:x8) -> void;
proc main() -> void {
  let color: Color =  Color {10,20,30,40};
  let x: x8 = color.r + 10;
  // printf("color {r: %d, g: %d, b: %d, a: %d};", color.r, color.g, color.b, color.a);
}
